// A linked list ia a form of a tree
// full tree -> every item either points to 2 nodes or 0 nodes
// perfect tree -> means that every line that has items in it is completely filled all the way to across
// complete tree -> means that every level is filled except for the last level
// can have more thatn one parent
// a node that does not have any children is called a leaf
//   