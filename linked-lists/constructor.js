class LinkedList {
  // Creates a new node
  constructor(value) {}
  // Creates a new node and adds it to the end of the list
  push(value) {}
  // Creates a new node and adds it to the beginning of the list
  unshift(value) {}
  // Creates a new node and adds it to whatever index is passed
  insert(index, value) {}
}

// So, they all creates a node